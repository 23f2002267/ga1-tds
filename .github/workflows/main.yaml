name: CI Pipeline

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest  # Required: defines the runner environment
    steps:
      - name: 23f2002267@ds.study.iitm.ac.in
        run: echo "Hello, world!"
